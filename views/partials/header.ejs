<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.1.0/fonts/remixicon.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <% if (nav === true) { %>
      <div class="w-full px-10 py-3 flex justify-between text-white bg-zinc-700 items-center">
        <h3>Pinspiration</h3>
        <div class="flex gap-10 items-center ">
          <a href="/profile">Profile</a>
          <a href="/feed">Feed</a>
          <form action="/logout" method="post">
            <button type="submit" class="px-4 py-2 bg-red-700 text-white rounded-full"
            >Logout</a
          >
          </form>
        </div>
      </div>
    <% } %>
